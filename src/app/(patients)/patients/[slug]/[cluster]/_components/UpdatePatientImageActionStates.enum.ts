export enum UpdatePatientImageActionStates {
  IDLE = 'idle',
  SUCCESS = 'success',
  FAILED = 'failed',
  PATIENT_IMAGE_DOES_NOT_EXISTS = 'patient_image_does_not_exist',
  INVALID_DATA = 'invalid_data',
}
