export enum AddPatientActionStates {
  IDLE = 'idle',
  SUCCESS = 'success',
  FAILED = 'failed',
  PATIENT_EXISTS = 'patient_exists',
  INVALID_DATA = 'invalid_data',
  PATIENT_IN_TRASH = 'patient_in_trash',
}
