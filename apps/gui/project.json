{
  "name": "gui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/gui/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project gui --web",
  "targets": {
    "all": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run gui:preview", "nx run be:serve"],
        "parallel": true
      }
    }
  }
}
